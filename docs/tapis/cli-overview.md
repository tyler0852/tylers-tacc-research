# Tapis CLI
 
## How to Use

```bash
cargo run -- --help
cargo run -- healthcheck --help
cargo run -- auth-hello --help
cargo run -- healthcheck
cargo run -- auth-hello
cargo run -- --base-url https://tacc.tapis.io healthcheck
cargo run -- --base-url https://tacc.tapis.io auth-hello
cargo run -- bogus
```